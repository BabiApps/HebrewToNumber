[pytest]
testpaths = tests
addopts = -ra
markers =
    decimals: tests for 'נקודה' (decimal phrases)
    fractions: tests for 'חצי/רבע' (including ו חצי/ו רבע)
    strict: strict invalid-grammar behavior
